# SEO PDCA レポート

**実行日：2026-07-25** ／ モード：**初期（ベースライン記録）** ／ GA4：取得済（ブラウザ）・GSC：取得済（ブラウザ）

> 注：GSC データは Ahrefs MCP からは取れなかった（Ahrefs に proofloop.jp のプロジェクトが未登録＝CEO対応待ち）。同席ブラウザの GSC 画面から直接取得した。取得期間は GSC=過去3ヶ月（2026-04-23〜07-22）、GA4=過去7日（GA4ホーム既定）。**初回のため前期比は取れない。** 次回（28日後 ≈ 2026-08-22）から比較が意味を持つ。

## 前回の提案の結果

初回のため対象なし。

## 全体サマリー（今回のベースライン）

| ソース | 指標 | 値 |
| --- | --- | --- |
| GSC（3ヶ月） | クリック / 表示 / CTR / 平均順位 | 6 / 63 / 9.5% / 14.3 |
| GA4（7日） | アクティブユーザー / 新規 / イベント / キーイベント | 34 / 28 / 554 / 0 |
| インデックス | 登録ページ数 | 2（2026-07-25 に sitemap 初投入。主要4ページは登録リクエスト済み） |

**一言：まだ検索での実体がほぼ無い。** GSC のクリックは指名（`proofloop`）中心、GA4 の流入も大半が Direct とインフラ由来のノイズ。**「悪い」のではなく「始まっていない」**状態で、今日がスタート地点。

## ページ別スコアカード

GSC はサイト全体で 6 クリックしかなく、その大半は指名クエリ→トップページ。ページ別のオーガニックはほぼ全ページ「流入ゼロ」。GA4 の表示回数（7日）で見た主要ページ：

| ページ（GA4タイトル） | GA4表示(7d) | オーガニック(GSC) | フラグ |
| --- | --- | --- | --- |
| トップ（`/`） | 195 | 指名クリックの受け皿 | 診断（指名以外の入口が無い） |
| 学生団体を探す（`/search`系・団体） | 50+ | ほぼ0 | 流入ゼロ |
| サークルを探す（`/guide/circle`） | 19 | ほぼ0 | 流入ゼロ |
| バイトガイド（`/baito`） | 16 | ほぼ0 | 流入ゼロ |
| 新入生ガイド（`/guide`） | 14 | ほぼ0 | 流入ゼロ |
| 授業レビュー（`/classinfo`） | 7 | ほぼ0 | 流入ゼロ |

※ GSC 上位クエリ：`proofloop`(5click/7imp)、`学生団体 就活`(0/4)、`学生団体`(0/2)、`大学 選択科目 落とす gpa`(0/1)、`一橋大学`(0/1)。**表示は出始めているがクリックに至っていない**＝まだ順位が低い（平均14.3位）。

## 次にやること（インパクト×手間ランク）

1. **【インパクト大・手間小】インデックスの継続監視。** 本日 sitemap を初投入し、登録はまだ2ページ。主要4ページ（money/credits/living-alone/baito）は登録リクエスト済み。**次回、登録ページ数が増えているか・団体ページ(2,400+)が吸収され始めたかを確認**する。ここが増えない限り何も始まらないので最優先の観測点。（根拠：`docs/owner-todo.md` の GSC 記録、今回の登録2ページ）

2. **【インパクト大・手間小】GA4 のデータ衛生を整える。** 現状の流入は `vercel.com/referral`（デプロイプレビュー）4件、`(direct)` 35件、Netherlands 9 など**開発・インフラ・ボット由来のノイズが実ユーザーを埋もれさせている**。内部トラフィック除外（開発者IP/自社アクセス）と Vercel 参照の除外設定を入れないと、**今後の全 PDCA のベースラインが歪む**。今のうちに設定すべき。（根拠：GA4 チャネル Direct 35 / Unassigned 10、参照元 vercel.com 4）

3. **【インパクト中・手間中】表示はあるがクリック0のクエリにページが対応しているか点検。** `学生団体 就活`(表示4)・`学生団体`(表示2) は需要の兆し。ただし**就活系はマネタイズ対象にしない**（CLAUDE.md §5：就活アフィリはiBECK競合＝除外／§2：収益はB2B協賛）。よって「就活」は情報提供・ブランド面のみで扱い、アフィリ導線は張らない。まず該当意図に応えるページ/セクションが存在し内部リンクが通っているかを確認する。（根拠：GSC クエリ表）

4. **【インパクト小・手間小】AI検索チャネルを観測開始。** `chatgpt.com / ai-assistant` から1セッション発生。AI アシスタント経由の被参照は新チャネル。次回、この流入の推移を定点観測する（伸びるなら診断系・ガイドの被引用を意識した構成が効く）。（根拠：GA4 チャネル「AI Assistant」1）

## データ成熟度・インデックスメモ

- **初期モード**：GSC 総クリック6・GA4 実ユーザー僅少。今回は**ベースライン記録**が目的で、順位改善提案は無理に出していない。
- sitemap 初投入日 = 2026-07-25。登録2ページ→主要ページ登録リクエスト済み。
- 次回実行（≈2026-08-22）から、GSC/GA4 とも**前期比**が意味を持つ。特に「インデックス数の伸び」と「オーガニッククリックの発生」を最初の成長シグナルとして見る。
