# ProofLoop リスク台帳

> 法務・セキュリティ・レピュテーションのリスクを一覧し、誰が判断するかを明示する。
> 設計の背景は `docs/superpowers/specs/2026-08-06-legal-foundation-design.md`。
>
> **運用ルール**
> - 状態は `未対応` / `対応中` / `✅対応済み` / `該当なし` のいずれか
> - 「判断者」が `オーナー` `専門家` のものは Claude が勝手に進めない
> - 新しいリスクに気づいたら、根拠（ファイル名と行番号、または実測値）を添えて追記する

**最終更新：2026-08-09**

---

## 1. 法務（L）

> ⚠️ **L1・L2・L3・L8・L10 は実装は完了しているが、文面のオーナー承認は未了。**
> 公開した4文書は実装と実態から起こした草案であり、`docs/owner-todo.md` に確認依頼を起票済み。
> 特に `/about` の全国学生団体連盟に関する記述と、`/privacy` の第三者提供の記述は事実確認が要る。

| ID | 事象 | 根拠 | 影響 | 緊急 | 判断者 | 推奨対応 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| L1 | 利用規約・プライバシーポリシーが存在しない。氏名・大学・学部・入学/卒業年・メール2種を取得しているのに利用目的の公表がない | コード全文検索で「プライバシー」「利用規約」の該当0件（2026-08-06） | 高 | 高 | Claude実装 | `/terms`・`/privacy` を作成し、フッターと登録画面から導線を通す | ✅対応済み（2026-08-06） |
| L2 | 運営者表示がない。誰が運営しているかサイト上で辿れない | `app/` に about / company 相当のページなし | 中 | 高 | Claude実装 | `/about` を作成し、会社情報を明示する | ✅対応済み（2026-08-06） |
| L3 | 掲載団体の削除依頼窓口がない。2,421件を同意なく掲載しているのに止める手段を提示していない | フッターの問い合わせが団体ページへの外部リンクで、メールアドレスがサイト上に存在しない（`components/Footer.tsx:19-20`） | 高 | 高 | Claude実装 | `/listing-policy` と窓口アドレスを明示し、団体ページからも導線を張る | 🔴**再オープン（2026-08-07）**。画面の実装は完了したが、**窓口 `contact@proofloop.jp` が受信できない**（オーナー申告）。届かない窓口は書いていないのとほぼ同じで、実質未対応。DNS は正常（MX＝`smtp.google.com`）で原因は Google 側。`docs/owner-todo.md` 🔴 参照 |
| L8 | 取得した学生データの利用目的が未定義。母体が就活事業者のため目的外利用を疑われやすい構造 | `app/signup/page.tsx`（氏名・大学・学部・入学/卒業年を取得）、CLAUDE.md §5 | 中 | 高 | Claude起案＋オーナー確認 | プライバシーポリシーに「就活事業へ本人同意なく提供しない」を明示 | ✅対応済み（2026-08-06） |
| L10 | 外部委託先と越境移転が未開示。Supabase・Vercel・Resend・Google アナリティクスはいずれも米国事業者 | `docs/accounts-inventory.md` | 中 | 高 | Claude実装 | 委託先と所在国を一覧で開示する | ✅対応済み（2026-08-06） |
| L5 | **職業安定法上の位置づけ。** `/clubats`（採用管理）・`/companysearch`（企業が学生を探す）・`job_listings`（求人掲載）が「職業紹介」または「募集情報等提供事業」に当たるか | 実装済み機能。`job_listings` は本番に18件。**代表の竹中が職業紹介の許可を保有（2026-08-06 オーナー申告）** | 中 | 中 | オーナー | ✅ **解消（2026-08-15）**。許可番号 `13-ユ-314800`（令和4年12月1日・**株式会社iBECK名義**）を確認。個人名義の懸念は外れた。残作業：`/about`への許可番号掲載（Claude実装可能） | ✅対応済み（2026-08-15） |
| L4 | 早稲田463件の一括収集。著作権・データベース権。事実データのみで説明文は非取込なのは軽減材料 | `scripts/waseda/`、CLAUDE.md §5「スクレイピング禁止」と乖離 | 中 | 中 | **オーナー＋専門家** | 収集方式の是非と、CLAUDE.md の方針文を実態に合わせるかを判断する | 未対応 |
| L6 | 特定電子メール法。団体宛メール施策の前提（送信者の表示義務・オプトアウト導線） | `docs/superpowers/specs/2026-07-29-team-roadmap-tool-design.md` §5 | 中 | 中 | オーナー（着手前） | 公表アドレスへの送信が例外規定に当たるかを確認し、表示義務を満たす文面を用意する | 未対応 |
| L9 | 未成年の扱いが規約にない | 該当記載0件 | 低 | 低 | Claude実装 | 利用規約に法定代理人の同意に関する条項を置く | ✅対応済み（2026-08-06） |
| L7 | ステマ規制（景品表示法）。広告に広告である旨の表示が要る | `lib/guide/` の `findMislabeledAffiliateLinks` テストで担保。`rel="sponsored"`／「※広告」を表示 | — | — | — | — | ✅対応済み |
| L11 | **電気通信事業法上の届出（第16条）が未了の可能性。** DM・チャット機能（`ChatRoom.tsx`／`application_messages`、学生⇔団体のメッセージ機能。`/clubmessages` `/mypage/messages` `/clubats` から利用）は「他人の通信を媒介する」機能に当たり、営利性の有無に関わらず届出対象になりうる | 2026-08-14 オーナー指摘を受けて調査。総務省・専門家サイト複数を確認（`docs/owner-todo.md` の当該項目に出典URLあり）。**サービスは既に本番稼働中**のため、届出未了のまま運用している状態 | 中〜高 | 中 | **オーナー＋専門家** | 対象サービスに該当するか弁護士・行政書士に確認のうえ、該当すれば管轄の総合通信局（情報通信部電気通信事業課）へ届出書（電気通信事業法第16条第1項・施行規則第9条第1項第8号・様式第2の2）を提出する。届出を怠り事業を営んだ場合、6ヶ月以下の懲役または50万円以下の罰金の規定がある | 未対応 |

---

## 2. セキュリティ（S）

| ID | 事象 | 根拠 | 影響 | 緊急 | 判断者 | 推奨対応 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S1 | `middleware.ts` が無く `/admin` にサーバーサイドゲートが無い。管理画面のHTML/JSが誰にでも配信される | `middleware.ts` 不在、`app/admin/page.tsx:73-76` の `useEffect` 内クライアント判定 | 中 | **高**（2026-08-09 引き上げ） | Claude実装 | ✅ **対応済み（2026-08-10）。** `middleware.ts` で `/admin` 配下に HTTP Basic 認証を追加（判定は `lib/auth/basicAuth.ts` の純粋関数・テスト18件）。⚠️ **Vercel の環境変数 `ADMIN_BASIC_USER` / `ADMIN_BASIC_PASSWORD` 設定が前提**（`docs/owner-todo.md`）。未設定のまま本番へ出すと `/admin` が 503 になる（fail closed は意図的）。<br>**再調査で分かったこと：情報漏えいではなかった。** `/admin` 配下のデータはすべて RLS と RPC 内の `is_system_admin()` で守られており、`/admin/disputes` の通報者PIIも `list_open_disputes()` の内部チェックを通る。S1 の実体は多層防御の欠落。<br>**Supabaseセッションをmiddlewareで見る案は不採用。** `lib/supabase.ts` が素の `createClient` でセッションが localStorage にあり、middleware は Cookie しか読めない。移行には `@supabase/ssr` が必要で全ページの認証経路に波及し、既存ログイン中の利用者が全員ログアウトされる | ✅ 対応済み（環境変数の設定待ち）<br>`/admin/disputes`・`/admin/claims`の新セクション（承認済み・却下済み一覧）を含め、`matcher`のパスベース網羅により追加対応不要と確認済み（2026-08-13）。 |
| S2 | パスワード規則が三重に不整合。学生は手書きチェックで8文字、zodスキーマは6文字、企業は6文字。さらに**ログイン画面が最小長を検証しており**、規則を引き上げると古い規則で登録した利用者が正しいパスワードでも締め出される事故につながる状態だった | `app/signup/page.tsx:27`（zod min 6）／`:102`（手書き8）／`:470`（企業6）／`app/login/page.tsx:55`（6） | 低 | 低 | Claude実装 | `lib/auth/password.ts` に閾値・文言・検証を集約し**新規登録を8文字に統一**。プレースホルダも定数から生成。**ログイン側の長さ検証は削除**（正否はサーバーが判定するため意味が無く、締め出しリスクだけが残るため）。テスト9件 | ✅対応済み（2026-08-06） |
| S3 | RLSが唯一の防壁。ポリシーの抜けがそのまま情報漏えいになる | マイグレーション021・023の `profiles.role = 'admin'` 判定 | 低 | 中 | Claude運用 | `get_advisors(security)` の定期確認を運用に載せる | 未対応 |
| S4 | 環境変数の秘匿 | `.gitignore:30` の `.env*.local` で除外、`git ls-files` で未追跡を確認（2026-08-06） | — | — | — | — | ✅問題なし |
| S5 | 領収書画像の保護 | マイグレーション026。Storage `finance-receipts` は非公開バケット、表示は署名付きURL | — | — | — | — | ✅対応済み |
| S6 | **claim トークンの漏洩。** DMの転送・アカウント譲渡で第三者に渡りうる | `/claim/[token]`、`organization_claims.token`（単回使用） | 中 | 低 | Claude運用 | 単回使用・90日期限・運営承認（`decide_claim`）・剥奪経路（`revoke_claim`）で緩和済み | 対応済み（残存リスクあり） |
| S7 | **［既存・claim実装中に発見］`profiles.role` を本人が書き換えて admin へ自己昇格できた。** UPDATE/INSERTポリシーのWITH CHECKがnull・role列にauthenticated/anonがテーブル権限を持つ・CHECK制約もトリガーも0件（本番実測 2026-08-07）。028/029が admin に「団体オーナー権を配る力」を与えるため、これを塞ぐまで028/029は適用しなかった | マイグレーション030（`5938302`→`2c89a9b`） | 高 | — | — | 列レベルGRANTでrole列だけ閉じる（029のorganizations.user_idと同じ手法） | ✅対応済み（2026-08-08） |
| S8 | **［既存・claim実装中に発見］`organizations.user_id` を書き換えて応募者PIIに到達できた。** authenticatedがorganizations.user_idをUPDATEでき、その列を条件にするポリシーがapplications・application_messages・profiles（応募者PII）など6テーブル8本ある。claimでownerになった者がuser_idを自分に書き換えれば、limited承認でも応募者の個人情報に到達でき、剥奪しても残っていた | マイグレーション029（`1d30fcc`） | 高 | — | — | テーブルレベルUPDATEをrevokeし、団体が編集してよい列だけを列レベルでgrantし直す | ✅対応済み（2026-08-08） |
| S9 | **［既存・claim実装中に発見］未認証で全招待トークンを列挙できた。** `organization_invitations` のSELECTポリシー「Anyone can view invite by token」がqual:trueで、未認証でも全行読めていた。当時0件で実害は未発生だったが、団体が招待を使い始めた瞬間に全トークンが漏れる状態だった | マイグレーション028（`fea7a0b`） | 高 | — | — | 該当ポリシーをDROP。受諾は`get_invitation_preview` RPC経由のみに一本化（`organization_invitations_select_org_admins`で管理画面の一覧・招待APIは通ることを確認済み） | ✅対応済み（2026-08-08） |
| S10 | **`events` の書き込みが全開。** `authenticated` なら誰でも任意の団体のイベントを作成・更新・削除できる | 本番 `pg_policies` を実測（2026-08-12・D9 の調査中に発見）。`events_insert_policy` / `events_update_policy` / `Users can update their own events` / `events_delete_policy` がいずれも `qual: true`。同居する `Club admins can update their own events`（`organizations.user_id` 起点）は permissive なので何も絞っていない。claim とは無関係の既存問題 | 中 | 中 | Claude実装 | `organization_id IN (SELECT get_user_organization_ids(auth.uid()))` 起点に締める（`tasks` の `tasks_*_own_org` と同じ形）。SELECT は公開のままでよい。**D9・D10 とは別件なので 035 には含めていない** | 未対応 |
| S11 | **`organization_members_role_check` が `'member'` を許しておらず、claim の「限定」承認が本番で一度も成立しない状態だった** | 本番 `pg_constraint` を実測（2026-08-12・D9 の検証中に発見）。033 の `decide_claim` は `p_level='limited'` で `role='member'` を書くが、制約は `owner`/`admin` のみ。メンバー行の INSERT が `23514 check_violation` で落ち、関数の EXCEPTION 節は `unique_violation` しか捕まえないため運営に 500 が返る。TS側（`lib/organizationMembers.ts:149`）は `member` 前提で書かれており、DBだけが追随していなかった | 中 | 中 | Claude実装 | マイグレーション035 で `'member'` を許す。`organization_invitations` 側の同名制約は緩めない（招待で限定相当を配る導線が無いため）。**claim を1件も承認していないため実害なし** | ✅対応済み（035・⚠️本番適用待ち） |
| S12 | **ログインしている全ユーザーが、全ユーザーの `profiles` 行（`email`・`contact_email`・`full_name`・`university` など）を読める** | 本番実測（2026-08-12・035 のレビュー中に発見）。`profiles` の SELECT ポリシー `Public profiles are viewable by everyone` が `roles={authenticated}` / `qual: true`。未ログイン（anon）は影響外。claim とは無関係の既存問題 | **高** | 中 | オーナー承認済み・Claude実装 | ✅ **対応済み（2026-08-12・マイグレーション037）。** 列権限には触れず（030の教訓を踏まえ、`email`は本人・団体メンバー↔応募者の関係で正当に必要なため）、無条件ポリシーを1本削除するだけで直した。削除前に依存を発見：`get_owner_user_ids_for_applied_orgs`が`role='owner'`のみを対象にしていたため、限定承認（role='member'）の団体宛の通知メール（応募完了・応募DM）がこの無条件ポリシーに寄りかかっていた。同マイグレーションで関数のrole制限も外し、対称な形（`Clubs can view applicant profiles`と同じくrole不問）に揃えた。`BEGIN…ROLLBACK`で本人・同僚・応募者⇔団体（owner/member どちらでも）は影響なし、無関係の第三者だけ遮断されることを確認 | ✅対応済み（2026-08-12） |
| S13 | claim の「限定」承認は、実際には掲載編集・投稿に到達していた。「限定＝安全」ではなかった | 設計書 §10.5.6（本番 `pg_policies` 実測・2026-08-12）。`can_edit_profile`・`can_manage_posts`フラグを参照するRLSポリシーが1本も無く、デコレーションに過ぎなかった（033のC1・035と同型の穴） | 中 | 中 | オーナー承認済み・Claude実装 | ✅ **対応済み（2026-08-12・マイグレーション036）。** 「学生向けに出される情報（掲載内容・お知らせ投稿）は限定承認では書き換えられない」という基準でオーナー承認。`organizations`のUPDATEと`organization_posts`の書き込み系を`role IN ('owner','admin') OR フラグ`でゲートする判定関数を新設し、`decide_claim`の限定承認で`can_edit_profile`/`can_manage_posts`をfalseに変更。tasks・finance（学生向けではない）は対象外のまま。閲覧は制限しない。`BEGIN…ROLLBACK`で自作・フル承認は編集可、限定・第三者は編集不可、閲覧は誰でも可能なままを確認 | ✅対応済み（2026-08-12） |

---

## 3. レピュテーション（R）

| ID | 事象 | 根拠 | 影響 | 緊急 | 判断者 | 推奨対応 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R4 | 団体ページの説明文が空。「勝手に載せられた上に中身が無い」と見える。L3と連動する | 1,958件中1件のみ説明あり | 中 | 中 | オーナー | 事実データからdescriptionを生成する施策が保留中。権利面の判断待ち | 未対応 |
| R1 | 「学生団体」表記がサークル所属者を排除しうる | 18ファイルに出現していた | 中 | 中 | Claude実装 | 学生向け・団体向けの表示面を「**サークル・学生団体**」に統一。企業向け画面は「団体」に短縮。団体ページのdescription（2,421ページ）も中立化し、表記が戻らないようテストで固定。固有名詞「全国学生団体連盟」とミッション文は据え置き | ✅対応済み（2026-08-06） |
| R5 | `/for-clubs`・`/manual` の訴求不足と動線 | モックが灰色の箱・財務DXの記載なし・ドメイン誤記 | 中 | 中 | Claude実装 | 触れる会計デモを設置し、モックを具体的な再現に差し替え、機能紹介に会計を追加。`/manual` 冒頭と最終CTAに相互の導線を追加 | ✅対応済み（2026-08-06） |
| R2 | `/login` の「LINEでログイン」が死にボタン。`onClick` が無く押しても何も起きない | `app/login/page.tsx:167-173` | 中 | 低 | Claude実装 | ボタンと区切り線を削除する | ✅対応済み（2026-08-06） |
| R3 | `/for-students` が404なのにGA4で表示が発生している | GA4 28日（2026-07-09〜08-05）で6表示・4ユーザー | 低 | 低 | オーナー | **ページは作らない方針で決定（2026-08-06）。** 学生向けの入口は `/guide` ハブ・`/gpa`・`/baito` が担う。フッターのコメントに方針を明記した | ✅対応済み（2026-08-06） |
| R6 | 空ディレクトリの残骸 | `app/clubdashborad`・`app/clubopenmission`・`app/clubprofile`（いずれも中身なし。gitは空ディレクトリを追跡しないため実害なし） | 無害 | 低 | Claude | ローカルから削除する | 未対応 |
| R7 | **異議申立てによる妨害。** フォーム送信だけで団体を凍結できる（`submit_dispute` はanonから実行可） | 団体ページの申告窓口、マイグレーション032（`b1a3920`→`b47da9b`） | 中 | 低 | Claude運用 | 連絡先必須・同一団体1件（オープン申立ての重複防止）・自動凍結へのレート制限（1時間5件・超過分はopenで記録のみ）・運営へ即通知で緩和 | 対応済み（残存リスクあり） |

---

## 4. 該当なしと判断したもの（記録）

判断の経緯を残すため、検討した上で該当しないと結論づけたものも記録する。

| 分類 | 判断 | 理由 |
| --- | --- | --- |
| 資金決済法 | 該当なし | 財務DX（`/clubfinance`）は記録・集計のみで送金を行わない（`docs/superpowers/specs/2026-07-25-finance-dx-design.md` の決定5） |
| 特定商取引法 | 表示義務の直接の対象ではない | 物販・役務の通信販売を行っていない。ただし運営者表示は信頼性の観点から実施する（L2） |
