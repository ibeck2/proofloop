# オーナー対応事項（Claudeが実行できない作業）

> Claude Code から実行できない作業だけをここに集約します。
> 管理画面へのログイン、対人の依頼・確認、法務や事業上の判断が必要なものが対象です。
>
> **運用ルール**
> - 完了したら `- [ ]` を `- [x]` に変え、行末に `（完了 YYYY-MM-DD）` を付ける
> - 新しく発生した対応事項は該当の優先度セクションに追記する
> - Claude は作業開始時にこのファイルを読み、未完了項目があれば必要に応じて言及する

**最終更新：2026-07-23**

---

## 🔴 最優先（GPA計算機の公開直後にやる）

`/gpa`（GPA計算機）を 2026-07-22 に本番公開しました。狙っている `gpa 計算` は月間2,300検索・難易度0の穴なので、初動が効きます。

- [x] **`www.proofloop.jp` の CNAME 追加**（完了 2026-07-24・CEO対応）
- [x] **Google Search Console で `/gpa` のインデックス登録をリクエストする**（完了 2026-07-24）

- [x] **GSC にサイトマップを送信する**（完了 2026-07-25・Claude が claude-in-chrome で実施）
  - ⚠️ **実査で判明**：ドメインプロパティ `sc-domain:proofloop.jp` に **サイトマップが一度も送信されておらず、インデックス登録は2ページのみ**でした（旧記載「送信まで完了」は誤り）。
  - `https://proofloop.jp/sitemap.xml`（2,438 URL）を送信し、主要集客ページ（`/guide/money`・`/guide/credits`・`/guide/living-alone`・`/baito`）の個別インデックス登録もリクエスト済み。
  - 🔎 **オーナー確認（1〜2日後）**：GSC → サイトマップのステータスが「取得できませんでした」→「**成功**」に変わっているか／「ページ」レポートの登録数が増え始めているか。増えなければ Claude に伝えてください（再送信・原因調査します）。

- [x] **GA4 にカスタムディメンションを2つ登録する**（完了 2026-07-22・Claude が実施）
  - `metric_id` / `input_mode` をイベントスコープで登録済み。
  - あわせて本番 `https://proofloop.jp/gpa` で実際に計算を1回実行し、**GA4のリアルタイムに `gpa_calculate` イベントが届くことを確認**しました。計測経路は通っています。
  - ⚠️ **カスタムディメンションはレポートに反映されるまで24〜48時間かかります。** 登録前に発生したイベントには遡って適用されません（＝公開初日のデータには `metric_id` が付きません）。
  - 詳細は下の「実施済み」セクション参照。

<details>
<summary>（旧記載）GA4 カスタムディメンション登録の手順</summary>
  - 登録するもの（いずれも**イベントスコープ**）：
    - `metric_id` … どの指標で計算されたか（GPA / 基本平均点 / 成績評価係数を識別）
    - `input_mode` … 科目ごと入力 / まとめ入力
  - 手順：GA4 → 管理 → データの表示 → カスタム定義 → カスタムディメンションを作成
  - 理由：**登録しないと、送信はされているのにレポートで参照できません。** 上位校比率の測定という施策の目的そのものが達成できなくなります。
  - ⚠️ 注意：従来の `gpa_band` を `value_band` に置き換えました。**既存の `gpa_band` レポートは継続しません。** 過去データが必要なら、置き換え前後で期間を分けて扱ってください。

</details>

- [x] **`value_band` をカスタムディメンションに登録**（完了 2026-07-25・Claude が claude-in-chrome で実施）
  - 分析目的「上位校の成績帯分布」に必要なため登録。あわせて **`university_tier`（上位校比率の測定に直結）** も登録した（`gpa_calculate` が送信しているのに未登録だった）。
  - 現在のカスタムディメンションは4個：`metric_id` / `input_mode` / `value_band` / `university_tier`（50個上限に対し余裕）。
  - ⚠️ **カスタムディメンションは登録後に収集されたデータにのみ適用**され、過去データには遡及しません。value_band/university_tier のレポート反映は 2026-07-25 以降のイベントから。

- [x] **GA4：データ保持期間を14か月に変更**（完了 2026-07-25・Claude）
  - イベントデータの保持が初期値の「2か月」だったため「**14か月**」に変更（ユーザーデータは既に14か月）。過ぎたデータの探索分析が可能に。反映まで最大24時間。

- [x] **GA4：主要成果イベントをキーイベント（コンバージョン）化**（完了 2026-07-25・Claude）
  - `gpa_calculate`・`affiliate_click` をキーイベントに設定（従来キーイベントは0件だった）。
  - ⚠️ GA4がテンプレとして持っていた `close_convert_lead`・`qualify_lead`・`purchase` は**コードから一度も送信されていない空のキーイベント**。実害はないが、レポートを綺麗に保つなら外してよい（オーナー判断。将来ファネル用に残す選択も可）。
  - ⚠️ **キーイベントの集計はマークした時点以降**。過去のイベントは遡ってコンバージョン化されない。

- [x] **（コード改修）診断系にGA4イベントを実装**（実装完了 2026-07-25・Claude／**未デプロイ**）
  - 追加イベント：`simulator_complete`（score_band/wall_status/circle_level/credits/target_income）、`simulator_share`（share_platform＝x/line）、`study_abroad_complete`（top_country/top_region/purpose/period/budget/english_level/priority）。
  - 純粋関数＋テストは `lib/analytics/simulatorEvents.ts`・`studyAbroadEvents.ts`（既存 affiliateClick.ts と同パターン）。全177テスト緑・型チェック通過。
  - ✅ **本番デプロイ済み（2026-07-25）**。本番で各診断を1回実行し、**GA4リアルタイムに `simulator_complete`・`simulator_share`・`study_abroad_complete` の3イベントが届くことを確認済み**（計測経路OK）。
  - 🔜 **キーイベント化は保留中（GA4の制約待ち）**：`simulator_complete`・`study_abroad_complete` をキーイベント化したいが、このプロパティは「イベント一覧のスター付け」でしか設定できず、**発火直後の新イベントは一覧に反映されるまで最大24〜48時間かかる**ため今すぐ設定できない。一覧に出たら（＝翌日以降）スター1つで完了。Claude が対応可能なので、翌日以降にブラウザを開いた状態で依頼のこと。
  - 🔜 必要なら score_band 等を**カスタムディメンション登録**（任意）。

- [ ] **本番URLで最終目視する**
  - `https://proofloop.jp/gpa`
  - 確認するのは2点だけで十分：大学一覧に東大2エントリが出るか／実際に計算できるか
  - Claude が確認済みなのは `localhost` のみです。

---

## 🟡 今週中

- [ ] **Ahrefs に proofloop.jp を登録してもらう（CEOへ依頼）**
  - ワークスペース所有者が CEO（takenaka01@ibeck.co.jp）のため、**Claude 側からは操作できません**。
  - 2026-07-16 の会議時点で未完。完了済みならこの項目にチェックを入れてください。
  - 登録後、Rank Tracker に以下を追加：
    - `gpa 計算` / `大学 gpa`（既存施策の主軸）
    - `東大 gpa` / `基本平均点` / `成績評価係数`（今回の東大対応で新たに狙えるKW）
  - 理由：これがないと施策の効果を数値で追えません。

- [ ] **東京大学の教務窓口に2点確認する（できれば）**
  - 現在このツールで**唯一、出典に明記がなく推論で出している数値**です。画面上では「推論である」「提出先に確認を」と明示しているので誠実な状態ですが、確定できれば注記を外して断定でき、競合に対する優位がさらに強くなります。
  - ① **優上＝評価ポイント3 で正しいか**
    公式の成績評価係数計算表（`https://www.u-tokyo.ac.jp/content/400125968.xls`）に「優上」の列が存在しません。東大の他の公式ページが定める優上の点数帯（90点以上）が、計算表の100〜90点帯（評価ポイント3）と一致することから導いた推論です。
  - ② **成績評価係数で不可・F を分母に含めるか**
    同じファイル内で記載が矛盾しています。白紙シートの式は「総**登録**単位数」で除す（＝不可を含む）、記入例シートの手順説明は「不可やFは単位数に含めません」。現在は切替式にして既定を「含める」にしています。
  - 確認が取れたら Claude に伝えてください。データと注記を更新します。

---

## 🔵 アカウント棚卸しで判明した要対応（2026-07-22 追加）

詳細と確認手順は **`docs/accounts-inventory.md`** を参照。

- [x] **各サービスの所有アカウントを確定させる**（部分完了 2026-07-22）
  - 判明：**GA4 = `contact@proofloop.jp`** ／ **Resend = `ibeckzoom@gmail.com`** ／ **バリューコマース = `ibeckzoom@gmail.com`**
  - 残り未確認：**Search Console**（GA4と同じ `contact@proofloop.jp` の可能性が高い）／**Vercel**／**さくらインターネット**
  - 確認手順は `docs/accounts-inventory.md` §3。

- [ ] **Resend の独自ドメイン認証をする**
  - 現在、承認メール・招待メールの送信元が **`onboarding@resend.dev`**（Resendのテスト用共有アドレス）のままです。
  - 受信側には「ProofLoop運営 <onboarding@resend.dev>」と表示され、**信頼性を損ない、迷惑メール判定されやすくなります。**
  - CLAUDE.md の「学生団体への通知メールを営業＆流入の入口にする」施策を実行するなら、**これは前提条件**です。
  - 手順：Resend → Domains → proofloop.jp を追加 → 表示されたDNSレコードをさくらインターネットのゾーンに登録 → 認証後、コード側の `from` を差し替え（差し替えは Claude が対応します）。
  - **2026-07-23：`claude-in-chrome`（ブラウザ操作）で Claude が伴走する方針に決定。** オーナーが Resend とさくらにログインした状態のブラウザを用意していただければ、そこから先の操作・入力値の確認は Claude が行います。送信元は `contact@proofloop.jp` を予定。
  - ⚠️ SPFのTXTは1ドメインに1本しか置けません。既存の `v=spf1 include:_spf.google.com ~all` に Resend の include を**追記して1本にまとめる**必要があります（2本並べるとSPFが壊れます）。手順詳細は `docs/task-board.md` タスクD。

- [ ] **ドメイン `proofloop.jp` の更新期限を確認する**
  - さくらインターネットの会員メニューで確認。**失効するとサイトが即座に落ちます。**
  - 自動更新が有効か、支払い方法が生きているかもあわせて確認してください。

- [x] **Vercel のプロジェクト2つを整理する**（完了 2026-07-23・オーナーが `proofloop` プロジェクトを削除）
  - 本番は `proofloop-2cea`（proofloop.jp を配信）に一本化されました。

- [ ] **バリューコマース：提携申請8件の審査結果を確認する**（2026-07-24 申請・すべて「提携待ち」）

  | 広告主 | プログラムID | 承認後の反映先 |
  | --- | --- | --- |
  | タウンワーク | 2113716 | `/baito`：現在の公式リンクを**広告枠へ移行** |
  | シゴトin | 2054979 | `/baito`：新規カードを追加 |
  | マネーフォワード ME | 2174117 | `/guide/money`：家計簿の広告枠 |
  | GMOクリック証券 証券口座 | 2163260 | `/guide/money`：投資の入口 |
  | JCBカード | 2137100 | `/guide/money`：クレカ（要・注意喚起併記） |
  | リクルートカード | 2143285 | `/guide/money`：クレカ（同上） |
  | IIJmio（格安SIM） | 2134016 | `/guide/living-alone`：通信費の節約 |
  | アイリスプラザ | 2076736 | `/guide/living-alone`：一人暮らしの家具・家電 |

  **✅ 即時提携で成立済み・実装＆本番反映まで完了**（審査不要だった2社）
  | 広告主 | プログラムID | pid | 反映先 |
  | --- | --- | --- | --- |
  | エネチェンジ（電気・ガス比較） | 2143299 | 892665095 | `/guide/living-alone` 「電気・ガスを見直す」 |
  | ビレッジハウス（敷礼金ゼロ賃貸） | 2161824 | 892665096 | `/guide/living-alone` 「部屋を探す・契約の基礎」 |

  **見送り**：SoftBank光（2137376）… 規約が「**記事のファーストビューに広告表記**」を求めており、現行のカード単位開示より強い制約になるためオーナー判断で不採用。採用する場合はページ冒頭の広告表記の実装が前提。

  - **承認の連絡が来たら Claude に伝えてください。** MyLink取得 → `resources.ts` / `job_listings` へ投入まで対応します。
  - ⚠️ **除外方針（記録）**：FX（GMOクリックFX ¥11,000・松井FX ¥14,300）は報酬が高いが**投機性が高く学生の利益に反する**ため不採用。ビズリーチ等の就活競合も当然除外。保険相談も大学生の文脈に合わないため除外。
  - ⚠️ クレカ2件を掲載する際は、**借金リスクの注意喚起を必ず併記**する（信頼性ファースト原則）。
  - 💡 VC管理画面は数分で自動ログアウトします。ログイン時に「**次回から自動でログイン**」にチェックを入れると作業が途切れにくくなります。

- [x] **アフィリエイトを実装する**（完了 2026-07-24・Phase 0）
  - バリューコマース（sid:3766669）の**提携済みは3社のみ**：アルバイトEX・クラウドワークス・Yahoo!ショッピング。money適合（証券/クレカ/家計簿/奨学金）の提携は無し。
  - **baito**：`job_listings` に既にリンクは入っていた（owner-todoの「1本も無い」は誤り＝DBには有った）。ただしタウンワーク/シゴトin/ハタラクティブの3枚が名前と違う広告主に飛ぶ誤リンクだったため**非表示化**（本番Supabase反映済み）。現在はアルバイトEX＋クラウドワークスの2枚。
  - **living-alone**：Yahoo!ショッピング（開示付き広告）を「家電・家具」グループに追加してmainにマージ・push済み。
  - **money**：適合広告主が無いため公式リンクのみ（JASSO/金融庁）の方針を維持。
  - 収益拡大の次手：money/living-alone向けに証券・家計簿・引越し・ネット回線などを**VCで新規提携申請**すれば広告主を増やせる（審査待ちが発生）。

---

## 🟢 判断が必要（Claude が勝手に決められないもの）

- [x] **フッターのガイド導線を整備する**（完了 2026-07-23・UI刷新 第一周で実施）
  - `/gpa`・`/guide/credits`・`/guide/money`・`/guide/living-alone`・`/baito`・`/guide/study-abroad`・`/guide/circle` の7本をフッターに追加しました。
  - あわせてトップの「大学生活の疑問も、ここで解く」から `/guide` ハブへの導線も張っています。
  - ⚠️ 作業中に **`/for-students`（一般学生の方へ）が本番で404** であることが判明しました。旧フッターは全ページでこの死にリンクを出していたため、いったん**リンクを削除**しています。ページを用意するかどうかの判断をください（用意するなら Claude がフッターに復活させます）。

- [ ] **学生団体の画像収集の権利方式を決める**
  - CLAUDE.md の最優先タスクに残っていますが、**著作権・肖像権・ロゴ権の判断は Claude にはできません。**
  - 決めていただきたいのは方式です：「事前許可／オプトイン方式にする」「公式が公開している素材・許諾済みソースに限定する」など。
  - 方式が決まらないと設計に入れません（無断収集後の事後通知はリスクが高く、推奨しません）。

- [ ] **未追跡ファイル2件をどうするか決める**
  - `.claude/` … 設定ディレクトリ。コミットするか `.gitignore` に入れるか
  - `docs/proofloop-onboarding-2026-07-16.pdf` … 引き継ぎ資料。リポジトリに含めるか

- [ ] **問い合わせ用メールアドレス（info@ / support@）を用意する**
  - CLAUDE.md §9 に記載のある運用課題。ドメインメールの発行は Claude 側では行えません。
  - 用意できれば、種別振り分けと一次対応の自動化を設計できます。

---

## 参考：Claude 側で対応済み・対応可能なこと

以下はこのファイルの対象外です（Claude が実行できます）。

- コードの実装・テスト・レビュー・リファクタリング
- `git` 操作（コミット・ブランチ・マージ・push。**push は本番公開になるため毎回指示を仰ぎます**）
- Ahrefs の**データ取得**（MCP経由。ただしプロジェクト登録やRank Tracker設定などの管理画面操作は不可）
- Supabase のスキーマ確認・SQL実行（MCP経由。ただしスキーマ変更は CLAUDE.md §5 により計画提示と承認が必須）
- ローカルでのブラウザ検証（Chrome拡張が接続されている場合）
- 公式資料の調査と記録（`docs/seo/` 配下）
